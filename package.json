{
  "name": "scratch-next",
  "version": "1.0.0",
  "scripts": {
    "prepare": "husky install",
    "build": "pnpm --filter=scratch-gui run build",
    "start": "pnpm --filter=scratch-gui run start",
    "dev": "pnpm --filter=scratch-gui run dev",
    "commit": "commit"
  },
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3",
    "@commitlint/prompt-cli": "^18.4.3",
    "@commitlint/types": "^18.4.3",
    "husky": "^8.0.3"
  }
}
